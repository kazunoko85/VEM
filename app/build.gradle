//apply plugin: 'com.android.application'
apply plugin: 'android-library'

version  = '0.0.1'
group    = 'com.kazunokofactory.vem'



android {
    compileSdkVersion 20
    buildToolsVersion "20.0.0"

    defaultConfig {
        applicationId "com.kazunokofactory.vem"
        minSdkVersion 14
        targetSdkVersion 20
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            runProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}



dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

/*android-library
uploadArchives {
    repositories {
        mavenDeployer {
            repository url: "file://${your_repository_location}"
            pom.version = version
            pom.groupId = group
            pom.artifactId = 'vem-lib'
        }
    }
}
*/